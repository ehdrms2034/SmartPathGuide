name: Android CI

on:
  push:
    branches: 
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  test:
    name: Run Unit Tests
    runs-on: ubunto-latest
    steps:
      - name: Unit tests
        run: bash ./gradlew test --stacktrace
    
